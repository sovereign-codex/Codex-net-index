# SQLite Placeholder

Canon posture: staging placeholder.

This directory is reserved for a SQLite mirror database after separate authorization.

Current files define shape only and do not contain exported Notion data.
